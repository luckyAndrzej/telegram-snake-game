# Быстрая инструкция: Добавление проекта на GitHub

## 📦 Шаг 1: Подготовка

Убедитесь, что вы находитесь в директории проекта:

```bash
cd /Users/andrejkacinel/telegram_snake_game
```

## 🔷 Шаг 2: Создание репозитория на GitHub

1. Откройте [github.com/new](https://github.com/new)
2. Заполните:
   - **Repository name**: `telegram-snake-game`
   - **Description**: "Многопользовательская игра Змейка для Telegram"
   - **Visibility**: Private (рекомендуется) или Public
3. **НЕ ставьте галочки** на "Add README", "Add .gitignore", "Choose a license"
4. Нажмите **"Create repository"**

## 🚀 Шаг 3: Команды для загрузки

Скопируйте и выполните следующие команды (замените `YOUR_USERNAME` на ваш GitHub username):

```bash
# Инициализация Git (если еще не инициализирован)
git init

# Добавление всех файлов
git add .

# Создание первого коммита
git commit -m "Initial commit: Telegram Snake Game bot"

# Добавление remote репозитория (замените YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/telegram-snake-game.git

# Переименование ветки в main
git branch -M main

# Загрузка на GitHub
git push -u origin main
```

**Если используете SSH** (нужен настроенный SSH ключ):

```bash
git remote add origin git@github.com:YOUR_USERNAME/telegram-snake-game.git
git branch -M main
git push -u origin main
```

## ✅ Шаг 4: Проверка

1. Откройте ваш репозиторий на GitHub: `https://github.com/YOUR_USERNAME/telegram-snake-game`
2. Убедитесь, что все файлы загружены
3. Убедитесь, что файл `.gitignore` существует и `.env` файлы не загружены

## 🔐 Важно!

- ✅ Файл `.gitignore` должен содержать `.env`
- ❌ Никогда не загружайте токены в GitHub
- ✅ Используйте переменные окружения на платформе деплоя

## 📝 Для последующих изменений

После внесения изменений в код:

```bash
git add .
git commit -m "Описание изменений"
git push
```

---

Готово! Теперь переходите к [DEPLOYMENT.md](./DEPLOYMENT.md) для деплоя на Railway или Vercel.

