# Инструкция по развертыванию Telegram бота "Змейка"

Подробная инструкция по настройке проекта, добавлению на GitHub и деплою на Railway и Vercel.

## 📋 Содержание

1. [Подготовка проекта](#подготовка-проекта)
2. [Настройка GitHub](#настройка-github)
3. [Деплой на Railway](#деплой-на-railway) (Рекомендуется)
4. [Деплой на Vercel](#деплой-на-vercel) (Альтернатива)
5. [Настройка переменных окружения](#настройка-переменных-окружения)
6. [Получение токенов](#получение-токенов)
7. [Проверка работы](#проверка-работы)

---

## 🚀 Подготовка проекта

### Шаг 1: Установка зависимостей локально (для тестирования)

```bash
cd telegram_snake_game
pip install -r requirements.txt
```

### Шаг 2: Получение токенов

Перед деплоем нужно получить следующие токены:

#### A. Токен Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Змейка Game Bot")
   - Введите username бота (например: "snake_game_bot")
4. Скопируйте полученный токен (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
5. **Сохраните токен** - он понадобится позже

#### B. Токен Crypto Pay API

1. Откройте [@CryptoBot](https://t.me/CryptoBot) в Telegram
2. Отправьте команду `/start`
3. Выберите "💳 Crypto Pay"
4. Выберите "🔧 Мои приложения"
5. Нажмите "➕ Создать приложение"
6. Заполните форму:
   - Название: "Snake Game"
   - Описание: "Многопользовательская игра Змейка"
7. После создания скопируйте **API Token** (формат: `12345:ABCdefGHIjklMNOpqrsTUVwxyz`)
8. **Сохраните токен** - он понадобится позже

#### C. Telegram ID владельца

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Скопируйте ваш **ID** (число, например: `123456789`)
4. **Сохраните ID** - он нужен для получения комиссии

---

## 🔷 Настройка GitHub

### Шаг 1: Создание репозитория

1. Войдите на [GitHub](https://github.com)
2. Нажмите кнопку "New" (зеленая кнопка) или перейдите по ссылке [github.com/new](https://github.com/new)
3. Заполните форму:
   - **Repository name**: `telegram-snake-game` (или любое другое имя)
   - **Description**: "Многопользовательская игра Змейка для Telegram"
   - **Visibility**: Выберите Private (для безопасности) или Public
4. **НЕ** ставьте галочки на "Add README", "Add .gitignore", "Choose a license" (если файлы уже есть)
5. Нажмите "Create repository"

### Шаг 2: Инициализация Git и загрузка кода

Откройте терминал и выполните следующие команды:

```bash
# Перейдите в директорию проекта
cd /Users/andrejkacinel/telegram_snake_game

# Инициализируйте Git репозиторий (если еще не инициализирован)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Telegram Snake Game bot"

# Добавьте remote репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/telegram-snake-game.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/telegram-snake-game.git

# Загрузите код на GitHub
git branch -M main
git push -u origin main
```

**Важно**: Замените `YOUR_USERNAME` на ваш реальный GitHub username и `telegram-snake-game` на имя вашего репозитория.

### Шаг 3: Создание .gitignore

Создайте файл `.gitignore` чтобы не загружать секретные данные:

```bash
# Создайте файл .gitignore
cat > .gitignore << 'EOF'
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv

# Логи
*.log
bot.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Переменные окружения (ВАЖНО!)
.env
.env.local
config_local.py

# Railway
.railway/

# Временные файлы
*.tmp
*.bak
EOF

# Добавьте .gitignore в Git
git add .gitignore
git commit -m "Add .gitignore"
git push
```

---

## 🚂 Деплой на Railway (Рекомендуется)

Railway идеально подходит для Telegram ботов, так как поддерживает долгоживущие процессы.

### Шаг 1: Создание аккаунта

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите "Start a New Project"
3. Войдите через GitHub (рекомендуется)

### Шаг 2: Создание нового проекта

1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите ваш репозиторий `telegram-snake-game`
4. Railway автоматически определит Python проект

### Шаг 3: Настройка переменных окружения

1. В панели Railway выберите ваш проект
2. Перейдите на вкладку "Variables"
3. Добавьте следующие переменные:

| Переменная | Значение | Описание |
|------------|----------|----------|
| `TELEGRAM_BOT_TOKEN` | Ваш токен бота от BotFather | Токен Telegram бота |
| `CRYPTO_PAY_API_TOKEN` | Ваш токен от CryptoBot | Токен Crypto Pay API |
| `OWNER_ID` | Ваш Telegram ID | ID для получения комиссии |
| `PYTHON_VERSION` | `3.11` | Версия Python (опционально) |

### Шаг 4: Настройка деплоя

1. В разделе "Settings" найдите "Start Command"
2. Установите команду запуска:
   ```
   python main.py
   ```

3. (Опционально) В разделе "Build Command" можете добавить:
   ```
   pip install -r requirements.txt
   ```

### Шаг 5: Деплой

1. Railway автоматически начнет деплой при первом подключении
2. Дождитесь завершения деплоя (обычно 2-3 минуты)
3. Проверьте логи в разделе "Deployments" → "View Logs"
4. Если видите "Bot starting..." - бот запущен успешно!

### Шаг 6: Проверка работы

1. Откройте ваш бот в Telegram
2. Отправьте команду `/start`
3. Должно появиться приветственное сообщение

---

## ▲ Деплой на Vercel

Vercel больше подходит для serverless функций, но можно использовать для бота.

**⚠️ Важно**: Vercel имеет ограничения на время выполнения функций (10 секунд для бесплатного плана), что может быть проблемой для polling. Рекомендуется использовать Railway.

### Шаг 1: Установка Vercel CLI

```bash
npm install -g vercel
```

Или через Homebrew (macOS):
```bash
brew install vercel-cli
```

### Шаг 2: Создание конфигурации Vercel

Создайте файл `vercel.json`:

```json
{
  "version": 2,
  "builds": [
    {
      "src": "main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "main.py"
    }
  ],
  "env": {
    "PYTHON_VERSION": "3.11"
  }
}
```

### Шаг 3: Логин в Vercel

```bash
vercel login
```

### Шаг 4: Деплой проекта

```bash
# Перейдите в директорию проекта
cd telegram_snake_game

# Запустите деплой
vercel

# Следуйте инструкциям:
# - Set up and deploy? Y
# - Which scope? Выберите ваш аккаунт
# - Link to existing project? N
# - Project name? telegram-snake-game (или другое имя)
# - Directory? ./
# - Override settings? N
```

### Шаг 5: Настройка переменных окружения

1. Перейдите на [vercel.com/dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в "Settings" → "Environment Variables"
4. Добавьте те же переменные, что и для Railway:
   - `TELEGRAM_BOT_TOKEN`
   - `CRYPTO_PAY_API_TOKEN`
   - `OWNER_ID`

### Шаг 6: Передеплой

```bash
vercel --prod
```

---

## ⚙️ Настройка переменных окружения

### Для локальной разработки

Создайте файл `.env` в корне проекта:

```bash
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
CRYPTO_PAY_API_TOKEN=your_crypto_pay_token_here
OWNER_ID=your_telegram_id_here
```

**Важно**: Добавьте `.env` в `.gitignore` чтобы не загружать секретные данные на GitHub!

### Обновление config.py для использования переменных окружения

Убедитесь, что в `config.py` есть поддержка переменных окружения:

```python
import os

# Токен Telegram бота
TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN", "")

# Crypto Pay API токен
CRYPTO_PAY_API_TOKEN = os.getenv("CRYPTO_PAY_API_TOKEN", "111111111")

# ID владельца
OWNER_ID = int(os.getenv("OWNER_ID", "0")) if os.getenv("OWNER_ID") else None
```

---

## 📝 Получение токенов (Подробно)

### 1. Токен Telegram бота (@BotFather)

```
1. Откройте @BotFather в Telegram
2. Отправьте: /newbot
3. Введите имя бота: Змейка Game Bot
4. Введите username: snake_game_bot (должен заканчиваться на "bot")
5. Скопируйте токен: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

### 2. Токен Crypto Pay API (@CryptoBot)

```
1. Откройте @CryptoBot в Telegram
2. Отправьте: /start
3. Нажмите: 💳 Crypto Pay
4. Нажмите: 🔧 Мои приложения
5. Нажмите: ➕ Создать приложение
6. Заполните:
   - Название: Snake Game
   - Описание: Многопользовательская игра Змейка
7. Скопируйте API Token
```

### 3. Telegram ID (@userinfobot)

```
1. Откройте @userinfobot в Telegram
2. Отправьте: /start
3. Скопируйте ваш ID (число)
```

---

## ✅ Проверка работы

### 1. Проверка локально

```bash
# Установите зависимости
pip install -r requirements.txt

# Установите переменные окружения
export TELEGRAM_BOT_TOKEN="your_token_here"
export CRYPTO_PAY_API_TOKEN="your_token_here"
export OWNER_ID="your_id_here"

# Запустите бота
python main.py
```

Вы должны увидеть:
```
INFO:__main__:Bot starting...
```

### 2. Проверка на Railway

1. Откройте панель Railway
2. Перейдите в "Deployments"
3. Выберите последний деплой
4. Откройте "View Logs"
5. Должны увидеть: `INFO:__main__:Bot starting...`

### 3. Тестирование бота

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте `/start`
4. Должно появиться приветственное сообщение
5. Нажмите "🎮 Начать игру ($1)"
6. Проверьте создание счета на оплату

---

## 🔧 Решение проблем

### Проблема: Бот не отвечает

**Решение**:
1. Проверьте токен бота в переменных окружения
2. Проверьте логи на Railway/Vercel
3. Убедитесь, что бот запущен (проверьте логи)

### Проблема: Ошибка с Crypto Pay API

**Решение**:
1. Проверьте токен Crypto Pay API
2. Убедитесь, что токен правильный (не заглушка "111111111")
3. Проверьте формат токена

### Проблема: Бот не находит соперника

**Решение**:
1. Откройте бота в двух разных аккаунтах Telegram
2. В обоих аккаунтах отправьте `/start` и начните игру
3. Проверьте логи на наличие ошибок

### Проблема: Ошибка при деплое на Railway

**Решение**:
1. Проверьте файл `requirements.txt`
2. Убедитесь, что все зависимости указаны
3. Проверьте логи деплоя
4. Убедитесь, что команда запуска правильная: `python main.py`

---

## 📚 Дополнительные ресурсы

- [Документация Telegram Bot API](https://core.telegram.org/bots/api)
- [Документация Crypto Pay API](https://help.crypt.bot/crypto-pay-api)
- [Документация Railway](https://docs.railway.app)
- [Документация Vercel](https://vercel.com/docs)
- [Python Telegram Bot библиотека](https://python-telegram-bot.org)

---

## 🔐 Безопасность

1. **Никогда** не загружайте токены в GitHub
2. Используйте `.gitignore` для исключения `.env` файлов
3. Используйте переменные окружения для секретных данных
4. Регулярно обновляйте зависимости
5. Используйте приватные репозитории для продакшена

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи на платформе деплоя
2. Проверьте переменные окружения
3. Убедитесь, что все токены правильные
4. Проверьте документацию платформы деплоя

---

**Удачи с вашим ботом! 🐍🎮**

