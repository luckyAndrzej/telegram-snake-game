# Настройка Telegram Mini App

Инструкция по настройке и развертыванию веб-приложения для Telegram Mini App.

## 📋 Требования

1. Telegram Bot с токеном
2. Хостинг для веб-приложения (Railway, Vercel, или другой)
3. HTTPS домен (обязательно для Telegram Mini App)

## 🚀 Шаги настройки

### 1. Настройка бота

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите "Bot Settings"
5. Выберите "Menu Button"
6. Введите название кнопки: "Играть"
7. Введите URL веб-приложения: `https://your-domain.com/webapp`

### 2. Настройка переменных окружения

На Railway добавьте:

```
WEB_APP_URL=https://your-domain.com/webapp
```

### 3. Развертывание на Railway

#### Вариант 1: С Railway (рекомендуется)

1. Railway автоматически обнаружит Flask приложение
2. Добавьте команду запуска: `python main.py`
3. Убедитесь, что порт настроен правильно (по умолчанию 5000)
4. Railway автоматически предоставит HTTPS URL

#### Вариант 2: С отдельным сервером

Если веб-приложение на отдельном сервере:

1. Загрузите файлы из папки `webapp/` на сервер
2. Настройте веб-сервер (Nginx, Apache) для раздачи статических файлов
3. Обновите `WEB_APP_URL` в переменных окружения

### 4. Настройка веб-сервера (Nginx)

Пример конфигурации Nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location /webapp/ {
        alias /path/to/webapp/;
        try_files $uri $uri/ /webapp/index.html;
    }
    
    location /api/ {
        proxy_pass http://localhost:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 5. Получение HTTPS сертификата

Для Telegram Mini App обязателен HTTPS:

1. Используйте Let's Encrypt:
```bash
sudo certbot --nginx -d your-domain.com
```

2. Или используйте Railway, Vercel - они предоставляют HTTPS автоматически

## 🔧 Структура файлов

```
telegram_snake_game/
├── webapp/              # Веб-приложение
│   ├── index.html      # Главная страница
│   ├── styles.css      # Стили
│   ├── game.js         # Игровая логика
│   └── app.js          # Интеграция с Telegram
├── webapp_api.py       # Flask API для веб-приложения
└── main.py             # Основной файл бота
```

## 📝 API эндпоинты

- `POST /api/game/start` - Начать игру
- `POST /api/game/check-payment` - Проверить оплату
- `POST /api/game/status` - Проверить статус игры
- `POST /api/game/direction` - Отправить направление движения
- `POST /api/game/end` - Завершить игру
- `GET /webapp/` - Главная страница приложения

## 🎨 Особенности дизайна

- Современный темный дизайн
- Адаптивная верстка для мобильных и ПК
- Плавные анимации и переходы
- Интуитивное управление (кнопки, свайпы, клавиатура)
- Цветовая индикация статусов

## 🐛 Решение проблем

### Mini App не открывается

1. Проверьте, что URL правильный и доступен через HTTPS
2. Убедитесь, что в BotFather настроена кнопка меню
3. Проверьте логи сервера на ошибки

### Ошибки CORS

Добавьте домен в настройки CORS в `webapp_api.py`:

```python
CORS(app, resources={
    r"/api/*": {
        "origins": ["https://your-domain.com"]
    }
})
```

### Статические файлы не загружаются

1. Проверьте путь к папке `webapp/`
2. Убедитесь, что файлы существуют
3. Проверьте права доступа к файлам

## ✅ Проверка работы

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку "Играть" или найдите кнопку меню
4. Должно открыться Mini App с игрой

## 🔗 Полезные ссылки

- [Документация Telegram Mini Apps](https://core.telegram.org/bots/webapps)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Flask документация](https://flask.palletsprojects.com/)

