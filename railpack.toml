# Railway Railpack конфигурация для Node.js проекта

[build]
# Команда сборки
command = "npm install"

[deploy]
# Команда запуска
command = "node server.js"

# Явно указываем что это Node.js проект
[runtime]
type = "nodejs"
version = "18"
